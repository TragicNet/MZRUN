#ifndef GLOBALS_H
#define GLOBALS_H

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <dos.h>
#include <bios.h>
#include <string.h>
#include <dirent.h>
#include <time.h>

#define bool int
#define true 1
#define false 0
#define MaxX 60
#define MaxY 20
#define MaxSpeed 9
#define oTICKSPEED 100//30
#define oTIMELIMIT 100//200
#define totModes 4

int torchRange = 7;
int totLevels;
int levelCnt;
int wished = false;
bool returnTitle = false;
bool wait = false;
char cmap[MaxY][MaxX];
char paths[50][50];
bool pathsCleared[50];
enum eDirection { STOP = 0, LEFT, RIGHT, UP, DOWN };
char border[] = { 196, 179, 218, 191, 192, 217 };
unsigned int TICKSPEED = oTICKSPEED;
unsigned int TIMELIMIT = oTIMELIMIT;

char currentMode[20];
char Modes[totModes][20] = { "Normal", "Normal+", "Hard", "Hard+" };

void halt(int x) {   
    delay(x);
	while(1)
	{	if(kbhit())	getch();
		else	break;
	}
}

float distance(int xdiff, int ydiff) {
	unsigned int val = xdiff * xdiff) + ydiff * ydiff;
	if(val <= 0) {
		clrscr();	printf("Less");	getch();	exit(0);
	}
	return (val <= 0) ? 0 : sqrt(val);
}

#include "MZRUN/COORD.H"
#include "MZRUN/TILE.H"
#include "MZRUN/STATE.H"
#include "MZRUN/OBJECT.H"
#include "MZRUN/HELP.H"
#include "MZRUN/LEVEL.H"
#include "MZRUN/ENGINE.H"
#include "MZRUN/GAME.H"

#endif